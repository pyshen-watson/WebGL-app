class ModelSource {

    shift_base: number[]
    shift_grow: number[]
    degree_base: number[]
    degree_grow: number[]
    ratio: number[]
    axis: number[]

    vertexPositions: number[]
    vertexNormals: number[]
    vertexColors: number[]
}

export default ModelSource